Place your signed and notarized macOS DMG here and name it:

Abhishek-Admin.dmg

The public website download page uses `/downloads/Abhishek-Admin.dmg` by default.
You can override that with `VITE_ADMIN_DESKTOP_DOWNLOAD_URL`.
